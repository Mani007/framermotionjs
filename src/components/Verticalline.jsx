import React from 'react'

function Verticalline(props) {
  return (
    <>
<div className={`${props.height}  bg-slate-700 ${props.width} ${props.rotate} block`}>

    </div>
    </>
  )
}

export default Verticalline